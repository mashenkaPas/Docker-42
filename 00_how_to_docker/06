# docker run -d --name=overload -p 5000:80 --restart=always nginx 

#Map TCP port 80 in the container to port 5000 on the Docker host.
#thesame as p 192.168.1.100:5000:80

docker run -d -p 5000:80 --name=overlord --restart=always nginx
